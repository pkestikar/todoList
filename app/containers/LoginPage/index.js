import React from 'react';

const LoginPage = (props) => {
    return (
        <div style={{ background: 'black', color: 'white', fontSize: '6rem' }}>
            the login page
        </div>
    )
};

export default LoginPage;
